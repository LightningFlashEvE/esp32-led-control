# ESP32 构建速度优化指南

## 1. 编译器优化级别
当前使用调试模式，可以改为发布模式加速编译：

```bash
# 设置为发布模式（更快的编译和运行）
idf.py menuconfig
# 在菜单中找到：
# Compiler options -> Optimization Level -> Optimize for performance (-O2)
```

## 2. 禁用不必要的组件
在 `sdkconfig` 中禁用不需要的功能：

```
# 禁用蓝牙（如果不需要）
CONFIG_BT_ENABLED=n

# 禁用以太网（如果不需要）
CONFIG_ETH_ENABLED=n

# 禁用一些调试功能
CONFIG_ESP_SYSTEM_PANIC_PRINT_HALT=y
CONFIG_ESP_DEBUG_OCDAWARE=n
```

## 3. 使用增量构建
- 首次构建后，后续修改只会编译变更的文件
- 不要每次都执行 `idf.py fullclean`

## 4. 并行编译
```bash
# 使用多核并行编译（根据你的CPU核心数）
idf.py build -j8
```

## 5. 硬件优化
- 使用SSD存储
- 确保有足够的RAM
- 关闭杀毒软件实时扫描（暂时）

## 6. 典型构建时间
- 首次构建：3-8分钟（取决于硬件）
- 增量构建：10-30秒
- 仅修改main.c：5-10秒

## 7. 当前状态检查
你的配置显示：
- 使用调试模式（较慢）
- 包含完整ESP-IDF组件
- 建议首次构建完成后使用增量构建
